% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameters.R
\name{patch}
\alias{patch}
\title{Patch Parameter Values}
\usage{
patch(params, values)
}
\arguments{
\item{params}{An \code{lmm_params} object.}

\item{values}{A named list of updates (see Details).}
}
\value{
An updated \code{lmm_params} object.
}
\description{
Sets or updates values in an \code{lmm_params} object. Validates keys and values
immediately. Can be used both for initial fill and updates.
}
\details{
Accepted keys in the update list:
\itemize{
\item \code{beta}: named list of fixed effect values, e.g. \code{list(intercept = 0, x1 = 0.5)}
\item \code{dispersion}: single positive numeric
\item \code{random_sd}: named list per group, e.g. \code{list(id = list(intercept = 1, x1 = 0.3))}
\item \code{random_corr}: named list per group with either:
\itemize{
\item \code{list(structure = "cs", r = 0.3)} for shorthand structures
\item \code{list(matrix = m)} for a raw correlation matrix
}
}
}
